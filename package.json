{
  "name": "oneul-mwo-sseol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:firebase-rules": "npx --yes firebase-tools@15.24.0 emulators:exec --project demo-oneul-mwo-sseol --only firestore \"vitest run tests/firebaseRules.integration.test.ts\"",
    "test:e2e": "playwright test",
    "test:e2e:firebase": "npx --yes firebase-tools@15.24.0 emulators:exec --project demo-oneul-mwo-sseol --only auth,firestore \"playwright test --config playwright.firebase.config.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "firebase": "12.16.0",
    "phaser": "4.2.1",
    "qrcode": "1.5.4"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "@playwright/test": "1.62.0",
    "@types/node": "24.13.3",
    "@types/qrcode": "1.5.6",
    "typescript": "7.0.2",
    "vite": "8.1.5",
    "vitest": "4.1.10"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
